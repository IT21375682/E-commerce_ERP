<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Default state -->
    <item android:state_pressed="false">
        <shape android:shape="rectangle">
            <solid android:color="@color/primary" />
            <corners android:radius="20dp" />
            <stroke android:color="@color/primary" android:width="2dp" />
        </shape>
    </item>
    <!-- Pressed state -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/secondary" /> <!-- Change to the color you want when pressed -->
            <corners android:radius="20dp" />
            <stroke android:color="@color/secondary" android:width="2dp" /> <!-- Change to the color you want when pressed -->
        </shape>
    </item>
</selector>
